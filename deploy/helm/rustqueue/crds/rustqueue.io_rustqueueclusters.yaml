apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: rustqueueclusters.rustqueue.io
spec:
  group: rustqueue.io
  names:
    categories: []
    kind: RustQueueCluster
    plural: rustqueueclusters
    shortNames: [rq]
    singular: rustqueuecluster
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      schema:
        openAPIV3Schema:
          type: object
          required: [spec]
          properties:
            spec:
              type: object
              properties:
                image:
                  type: string
                  default: rustqueue:0.6.0
                imagePullPolicy:
                  type: string
                  default: IfNotPresent
                storage:
                  type: object
                  default:
                    className: ""
                    size: 100Gi
                    retainOnDelete: true
                    minFreeBytes: 10737418240
                    diskHighWatermarkPercent: 85
                    diskLowWatermarkPercent: 75
                    protectiveEvictionEnabled: true
                  properties:
                    className: {type: string, default: ""}
                    size: {type: string, default: 100Gi}
                    retainOnDelete: {type: boolean, default: true}
                    minFreeBytes: {type: integer, format: uint64, minimum: 0, default: 10737418240}
                    diskHighWatermarkPercent: {type: integer, format: uint8, minimum: 0, maximum: 255, default: 85}
                    diskLowWatermarkPercent: {type: integer, format: uint8, minimum: 0, maximum: 255, default: 75}
                    protectiveEvictionEnabled: {type: boolean, default: true}
                nodes:
                  type: object
                  default:
                    selector: {rustqueue.io/dedicated: "true"}
                    dedicated: true
                    taintKey: rustqueue.io/dedicated
                    failureDomainLabel: topology.kubernetes.io/zone
                    autoScaleFromNodes: true
                    replicas: null
                  properties:
                    selector:
                      type: object
                      additionalProperties: {type: string}
                      default: {rustqueue.io/dedicated: "true"}
                    dedicated: {type: boolean, default: true}
                    taintKey: {type: string, default: rustqueue.io/dedicated}
                    failureDomainLabel: {type: string, default: topology.kubernetes.io/zone}
                    autoScaleFromNodes: {type: boolean, default: true}
                    replicas: {type: integer, format: uint16, minimum: 0, maximum: 65535, nullable: true}
                cells:
                  type: object
                  default:
                    minNodes: 3
                    targetNodes: 9
                    maxNodes: 9
                    routersPerCell: 3
                    maxHomeCellsPerTopic: 128
                  properties:
                    minNodes: {type: integer, format: uint8, minimum: 0, maximum: 255, default: 3}
                    targetNodes: {type: integer, format: uint8, minimum: 0, maximum: 255, default: 9}
                    maxNodes: {type: integer, format: uint8, minimum: 0, maximum: 255, default: 9}
                    routersPerCell: {type: integer, format: uint8, minimum: 0, maximum: 255, default: 3}
                    maxHomeCellsPerTopic: {type: integer, format: uint16, minimum: 0, maximum: 65535, default: 128}
                replication:
                  type: object
                  default: {metadata: 3, partitions: 3}
                  properties:
                    metadata: {type: integer, format: uint8, minimum: 0, maximum: 255, default: 3}
                    partitions: {type: integer, format: uint8, minimum: 0, maximum: 255, default: 3}
                queue:
                  type: object
                  default:
                    defaultPartitions: 4
                    maxPartitionsPerTopic: 1024
                    maxMessageBytes: 33554432
                    maxBodyBytes: 67108864
                    maxBacklogMessagesPerPartition: 10000000
                    messageRetentionSeconds: 0
                    maxDeliveryAttempts: 16
                  properties:
                    defaultPartitions: {type: integer, format: uint16, minimum: 0, maximum: 65535, default: 4}
                    maxPartitionsPerTopic: {type: integer, format: uint16, minimum: 0, maximum: 65535, default: 1024}
                    maxMessageBytes: {type: integer, format: uint64, minimum: 0, default: 33554432}
                    maxBodyBytes: {type: integer, format: uint64, minimum: 0, default: 67108864}
                    maxBacklogMessagesPerPartition: {type: integer, format: uint64, minimum: 0, default: 10000000}
                    messageRetentionSeconds: {type: integer, format: uint64, minimum: 0, default: 0}
                    maxDeliveryAttempts: {type: integer, format: uint16, minimum: 0, maximum: 65535, default: 16}
                security:
                  type: object
                  default: {caValidityDays: 3650, certificateValidityDays: 365, renewBeforeDays: 30}
                  properties:
                    caValidityDays: {type: integer, format: uint32, minimum: 0, default: 3650}
                    certificateValidityDays: {type: integer, format: uint32, minimum: 0, default: 365}
                    renewBeforeDays: {type: integer, format: uint32, minimum: 0, default: 30}
                upgrade:
                  type: object
                  default:
                    automatic: true
                    maxUnavailablePerCell: 1
                    progressDeadlineSeconds: 600
                    retryGeneration: 0
                  properties:
                    automatic: {type: boolean, default: true}
                    maxUnavailablePerCell: {type: integer, format: uint8, minimum: 0, maximum: 255, default: 1}
                    progressDeadlineSeconds: {type: integer, format: uint64, minimum: 0, default: 600}
                    retryGeneration: {type: integer, format: uint64, minimum: 0, default: 0}
                resources:
                  type: object
                  default: {cpuRequest: 500m, memoryRequest: 1Gi, cpuLimit: "4", memoryLimit: 8Gi}
                  properties:
                    cpuRequest: {type: string, default: 500m}
                    memoryRequest: {type: string, default: 1Gi}
                    cpuLimit: {type: string, default: "4"}
                    memoryLimit: {type: string, default: 8Gi}
                development:
                  type: object
                  default: {allowSingleNode: false, virtualReplicas: 3}
                  properties:
                    allowSingleNode: {type: boolean, default: false}
                    virtualReplicas: {type: integer, format: uint16, minimum: 0, maximum: 65535, default: 3}
            status:
              type: object
              nullable: true
              properties:
                observedGeneration: {type: integer, format: int64, nullable: true}
                phase: {type: string, default: ""}
                allocatedReplicas: {type: integer, format: uint16, minimum: 0, maximum: 65535, default: 0}
                desiredReplicas: {type: integer, format: uint16, minimum: 0, maximum: 65535, default: 0}
                readyReplicas: {type: integer, format: uint16, minimum: 0, maximum: 65535, default: 0}
                pendingNodes: {type: integer, format: uint16, minimum: 0, maximum: 65535, default: 0}
                caRevision: {type: string, default: ""}
                cells:
                  type: array
                  default: []
                  items:
                    type: object
                    required: [id, desiredReplicas, readyReplicas]
                    properties:
                      id: {type: integer, format: uint64, minimum: 0}
                      desiredReplicas: {type: integer, format: uint16, minimum: 0, maximum: 65535}
                      readyReplicas: {type: integer, format: uint16, minimum: 0, maximum: 65535}
                conditions:
                  type: array
                  default: []
                  items:
                    type: object
                    required: [type, status, reason, message, lastTransitionTime]
                    properties:
                      type: {type: string}
                      status: {type: string}
                      reason: {type: string}
                      message: {type: string}
                      lastTransitionTime: {type: string}
                      observedGeneration: {type: integer, format: int64, nullable: true}
                upgrade:
                  type: object
                  nullable: true
                  required: [targetImage]
                  properties:
                    targetImage: {type: string}
                    currentNodeId: {type: integer, format: uint64, minimum: 0, nullable: true}
                    startedAt: {type: string, nullable: true}
                    paused: {type: boolean, default: false}
                    reason: {type: string, default: ""}
                    observedRetryGeneration: {type: integer, format: uint64, minimum: 0, default: 0}
