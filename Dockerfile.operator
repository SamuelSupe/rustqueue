FROM debian:bookworm-slim
RUN apt-get update && apt-get install -y --no-install-recommends ca-certificates && \
    rm -rf /var/lib/apt/lists/*
COPY .docker-bin/rustqueue-operator /usr/local/bin/rustqueue-operator
USER 65532:65532
EXPOSE 8080
ENTRYPOINT ["/usr/local/bin/rustqueue-operator"]
